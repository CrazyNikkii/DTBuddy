# Data Model: Main Navigation

This feature does not create, modify, or persist product data.

## Existing completed local match (read only)

The Profile → Match history action reuses the existing completed local match records. Their fields, sorting, and displayed values remain unchanged.

## Transient navigation selection

The currently selected main destination is temporary screen state. It is not a player preference and is not stored in the local database.
